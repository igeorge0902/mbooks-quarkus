package com.jeet.logging;

/**
 * Log level enum for {@link ObservedLog} annotation.
 * Ordinal order matches severity ascending (TRACE=0 ... ERROR=4).
 */
public enum LogLevel {
    TRACE, DEBUG, INFO, WARN, ERROR
}

